# React Recipe Manager

## Project Description
A simple React application that allows users to:
- View a list of recipes from a `recipes.json` data source
- Search recipes by name or ingredients
- Add/remove recipes to favorites

This project uses reusable components and **shadcn/ui** styled components.

---

## Setup / Installation Instructions

1. **Clone the repository**
```bash
git clone https://github.com/YOUR_USERNAME/react-recipe-manager.git
cd react-recipe-manager
Install dependencies

bash
Copy code
npm install
Install shadcn/ui components

bash
Copy code
npm install @radix-ui/react-primitive class-variance-authority tailwind-variants
Start the application

bash
Copy code
npm start
The app will run at http://localhost:3000.

